kind: Service
apiVersion: v1
metadata:
  name: ingress-nginx
  namespace: kairos
  labels:
    app.kubernetes.io/name: ingress-nginx
    app.kubernetes.io/part-of: kairos
spec:
  externalTrafficPolicy: Local
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: ingress-nginx
    app.kubernetes.io/part-of: kairos
  ports:
    - name: http
      port: 80
      targetPort: http
    - name: https
      port: 443
      targetPort: https
